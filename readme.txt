$ mkdir share
$ cd share
$ pwd
$ git init
$ git add readme.txt
$ git commit -m "wrote a readme file"
$ git status
$ git remote add origin https://github.com/stonejames/share.git
$ git push origin master
